# Core runtime dependencies for AutoSegmentor 3.0.0.

# torch/torchvision are pinned to a specific CUDA build (cu118). Unpinned ">="
# constraints let pip's resolver silently drift to a newer, non-CUDA wheel,
# which breaks torchaudio and any other package pinned to a cu118 build in a
# shared environment.
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.7.1+cu118
torchvision==0.22.1+cu118

numpy>=1.24
opencv-python>=4.8
opencv-python-headless>=4.8
PyYAML>=6.0
PyQt5>=5.15
ultralytics>=8.0
albumentations>=1.3
Pillow>=10.0
scipy>=1.10
matplotlib>=3.7
GPUtil>=1.4
google-genai>=1.0
google-generativeai>=0.8
onnx>=1.16
onnxruntime-gpu>=1.18
hydra-core>=1.1
omegaconf>=2.1
platformdirs>=4.0

# CoTracker3 is not installed via pip: it is vendored as the `external/co-tracker`
# git submodule and added to sys.path at launch. Run:
#   git submodule update --init --recursive

# flash-attn (CUDA speedup, optional) is intentionally NOT listed here: it requires
# torch to already be importable during its own build, so it must be installed
# after torch via `python install.py --cuda` or the `cuda` extra in pyproject.toml.
