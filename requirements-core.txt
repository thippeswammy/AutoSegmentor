# Core runtime dependencies for AutoSegmentor 3.0.0.

numpy>=1.24
opencv-python>=4.8
opencv-python-headless>=4.8
PyYAML>=6.0
torch>=2.0
torchvision>=0.15
PyQt5>=5.15
ultralytics>=8.0
albumentations>=1.3
Pillow>=10.0
scipy>=1.10
matplotlib>=3.7
GPUtil>=1.4
google-genai>=1.0
google-generativeai>=0.8
onnx>=1.16
onnxruntime-gpu>=1.18
hydra-core>=1.1
omegaconf>=2.1

# CoTracker3 is not installed via pip: it is vendored as the `external/co-tracker`
# git submodule and added to sys.path at launch. Run:
#   git submodule update --init --recursive

# Optional — CUDA speedup only, requires a matching CUDA toolchain/build tools.
# Also available as the `cuda` extra in pyproject.toml.
flash-attn>=2.0
